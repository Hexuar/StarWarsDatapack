$bossbar add $(id) {"translate": "gui.starwars.force_stamina"}
$bossbar set $(id) name {"translate": "gui.starwars.force_stamina", "color": "$(text_color)"}
$bossbar set $(id) color $(color)
$bossbar set $(id) max $(max)
$bossbar set $(id) players @s
$bossbar set $(id) value $(value)
$bossbar set $(id) visible $(visible)